frappe.listview_settings["Crate"] = {
  hide_name_column: true,
  //   get_indicator: function (doc) {
  //     return frappe.db.get_doc("Crate", doc.name).then((result) => {
  //       console.log(result.item_code);
  //       return [result.item_code, "green"];
  //     });
  //   },
};
